#!/bin/bash
# DEPRECATED: SessionStart hook removed — daemon logs config at startup instead.
# This script is a no-op stub for installs that haven't removed SessionStart from settings.json yet.
exit 0
